<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/25"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.mst_medical_app.controller.doctor.DoctorPatientsController"
      spacing="15" style="-fx-padding: 20;">

    <Label text="🩺 Danh sách bệnh nhân của bác sĩ"
           style="-fx-font-size: 22px; -fx-font-weight: bold;"/>

    <!-- Thanh tìm kiếm + thao tác -->
    <HBox spacing="10">

        <TextField fx:id="searchField"
                   promptText="🔍 Tìm theo tên hoặc địa chỉ..."
                   HBox.hgrow="ALWAYS"/>

        <Button fx:id="viewBtn"
                text="Xem thông tin"
                style="-fx-background-color: #3b82f6; -fx-text-fill: white;"/>

        <Button fx:id="noteBtn"
                text="Ghi chú bệnh"
                style="-fx-background-color: #10b981; -fx-text-fill: white;"/>

        <Button fx:id="historyBtn"
                text="Lịch sử khám"
                style="-fx-background-color: #6b7280; -fx-text-fill: white;"/>
    </HBox>

    <!-- Bảng hiển thị danh sách bệnh nhân -->
    <TableView fx:id="patientsTable"
               prefHeight="600"
               VBox.vgrow="ALWAYS"
               style="-fx-font-size: 16px; -fx-cell-size: 36px; -fx-background-radius: 10; -fx-border-color: #e5e7eb; -fx-border-radius: 10;">

        <columns>
            <TableColumn fx:id="colName" text="Họ tên" prefWidth="200"/>
            <TableColumn fx:id="colGender" text="Giới tính" prefWidth="100"/>
            <TableColumn fx:id="colDob" text="Ngày sinh" prefWidth="150"/>
            <TableColumn fx:id="colAddress" text="Địa chỉ" prefWidth="250"/>
            <TableColumn fx:id="colStatus" text="Tình trạng hẹn" prefWidth="150"/>
            <TableColumn fx:id="colNote" text="Ghi chú y tế" prefWidth="250"/>
        </columns>
    </TableView>

</VBox>
