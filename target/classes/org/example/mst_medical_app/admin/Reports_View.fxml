<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.chart.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/25"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.mst_medical_app.controller.admin.ReportsController"
      spacing="25" alignment="TOP_CENTER"
      style="-fx-padding: 20; -fx-background-color: #f9fafb;"
      VBox.vgrow="ALWAYS">

    <!-- Tiêu đề -->
    <Label text="📊 BÁO CÁO THỐNG KÊ HỆ THỐNG"
           style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #111827;"/>

    <!-- Hai biểu đồ tròn: Trạng thái & Giới tính -->
    <HBox spacing="40" alignment="CENTER">
        <PieChart fx:id="pieChartStatus"
                  title="Tỉ lệ cuộc hẹn theo trạng thái"
                  prefWidth="500" prefHeight="400"/>

        <PieChart fx:id="pieChartGender"
                  title="Tỉ lệ giới tính bệnh nhân"
                  prefWidth="500" prefHeight="400"/>
    </HBox>

    <!-- Biểu đồ cột -->
    <BarChart fx:id="barChart" prefWidth="1050" prefHeight="450"
              style="-fx-background-color: white; -fx-border-radius: 8; -fx-background-radius: 8;">
        <xAxis>
            <CategoryAxis label="Tháng"/>
        </xAxis>
        <yAxis>
            <NumberAxis label="Số lượng lịch hẹn"/>
        </yAxis>
    </BarChart>

    <!-- Chỉ số tổng -->
    <HBox spacing="60" alignment="CENTER">
        <Label fx:id="totalPatientsLabel" text="👥 Tổng bệnh nhân: 0"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #1f2937;"/>
        <Label fx:id="totalAppointmentsLabel" text="📅 Tổng cuộc hẹn: 0"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #1f2937;"/>
        <Label fx:id="totalRevenueLabel" text="💰 Doanh thu: 0 đ"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #1f2937;"/>
    </HBox>

</VBox>
